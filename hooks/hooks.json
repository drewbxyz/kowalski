{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "[ -f \"${user_config.wiki_root}/hot.md\" ] && cat \"${user_config.wiki_root}/hot.md\" || true"
          }
        ]
      }
    ],
    "PostCompact": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "[ -f \"${user_config.wiki_root}/hot.md\" ] && cat \"${user_config.wiki_root}/hot.md\" || true"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If files under the configured wiki root were created or edited this session AND a hot.md already exists at the top of that wiki root, verify the agent has rewritten hot.md this session (under 500 words, preserving its existing section shape). If the wiki root was untouched or hot.md does not exist, pass."
          }
        ]
      }
    ]
  }
}
